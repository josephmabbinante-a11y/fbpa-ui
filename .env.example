# Frontend
VITE_GOOGLE_MAPS_KEY=YOUR_KEY_HERE

# Backend MongoDB (choose one approach)
# 1) Full URI
MONGODB_URI=

# 2) Or component values (used when MONGODB_URI is empty)
MONGODB_USER=
MONGODB_PASSWORD=
MONGODB_HOST=
MONGODB_DB=fbpa-db
